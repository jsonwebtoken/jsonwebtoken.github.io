export interface ButtonsUiDictionaryModel {
  clearButton: {
    label: string;
  };
  syncButton: {
    label: string;
  };
  copyButton: {
    idle: {
      label: string;
    };
    done: {
      label: string;
    };
  };
  hideDetailsButton: {
    label: string;
  };
  showDetailsButton: {
    label: string;
  };
  minimizeButton: {
    label: string;
  };
  maximizeButton: {
    label: string;
  };
}
